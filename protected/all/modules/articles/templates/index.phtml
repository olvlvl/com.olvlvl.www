<?php

namespace ICanBoogie;

use App\Modules\Articles\Article;

/* @var $records Article[] */

foreach ($this as $date => $articles): ?>

	<h2 id="on-<?= $date ?>" class="text-muted"><?= DateTime::from($date)->format("Y, F") ?></h2>

	<?= $helpers['render']([ 'partial' => 'articles/overview', 'locals' => [ 'articles' => $articles ] ]) ?>

<?php endforeach;
